<% @events.each do |event| %>
      <tr>
        <td><%= event.name %></td>
        <td><%= event.description %></td>
        <td><%= event.date %></td>
        <td><%= event.time %></td>
        <%# if event.event_image.end_with?("jpg") %>
  <%#=image_tag event.event_image, width: 300, height: 200 %> 
  <%# <hr> %>
  <%#else%>
    <%#= image_tag eval(event.event_image)["url"], width: 300, height: 200 %> 
    <%# <hr> %>
  <%# end %>
<%= link_to 'More Details', group_event_path(@group.id) %>
<% end %>


<% @events.each do |event| %>
  <div class="card">
    <div class="cover" style="background-image: url(<%= image_url(event.event_image) %>)">
      <h1 class="event-card-title"><%= event.name %></h1>
      <h1 class="card-address"><% first_line_of_address = event.address.split(',').first.strip %>
        <%= first_line_of_address %></h1>
      <div class="card-back">
        <p class="event-card-detail">This is information about a bonsai</p>
        <a href="#">View detail</a>
      </div>
    </div>
  </div>
<% end %>




<%# style="background-image: url('<%= image_tag event.event_image %>
