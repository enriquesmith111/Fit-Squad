<%# <%= form_for [@event, EventParticipant.new], url: event_participants_path(@event) do |f| %>
  <%# <%= f.hidden_field :user_id, value: current_user.id %>
  <%# <%= f.submit "Join event" %>
<%# <% end %>
